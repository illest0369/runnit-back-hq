// Auth is now handled via APP_PIN and OPERATOR_USERNAME / OWNER_USERNAME environment variables.
// No credentials are stored in code or config files.
// See pages/api/login.ts for the single-operator auth implementation.

export {};
