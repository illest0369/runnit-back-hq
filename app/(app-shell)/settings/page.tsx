export const dynamic = 'force-dynamic'
export const revalidate = 0

import SettingsPageClient from '@/components/runnitback/SettingsPageClient'

export default function SettingsPage() {
  return <SettingsPageClient />
}
